[build]
  # Netlify will serve files from the "frontend" directory
  publish = "frontend"

# Optional: Set up redirects if you want to proxy API calls directly from Netlify 
# instead of setting API_BASE_URL in script.js. 
# Uncomment and change the URL if using this proxy approach.
# [[redirects]]
#   from = "/generate-speech"
#   to = "https://your-backend-url.com/generate-speech"
#   status = 200
#
# [[redirects]]
#   from = "/download/*"
#   to = "https://your-backend-url.com/download/:splat"
#   status = 200
#
# [[redirects]]
#   from = "/static/audio/*"
#   to = "https://your-backend-url.com/static/audio/:splat"
#   status = 200
